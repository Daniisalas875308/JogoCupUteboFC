/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://jogocup.com', // tu dominio principal
  generateRobotsTxt: true,       // genera también robots.txt
  changefreq: 'weekly',           // cada cuánto se actualizan las páginas
  priority: 0.8,
};
